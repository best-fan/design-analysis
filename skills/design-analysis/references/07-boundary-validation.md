---
title: 区域边界验证流程
impact: HIGH
impactDescription: 强制步骤，防止元素归属错误
tags: boundary, validation, coordinate, mandatory
---

# 区域边界验证流程

## 为什么执行此步骤？

**真实错误案例（2026-04-08）**：年份选择器归属错误

| 问题 | 错误操作 | 正确操作 |
|------|----------|----------|
| 年份选择器 x=546 | 凭视觉直觉判断属于右侧区域 | 验证坐标：160 ≤ 546 < 682，属于左侧区域 |

**后果**：文档将元素错误归类，导致前端开发实现错误的组件位置

---

## 区域边界验证模板

**在建立布局 Map 时，填写以下表格：**

```markdown
## 区域边界验证表（分析阶段必填）

| 区域名称 | x | width | 左边界 | 右边界 |
|----------|---|-------|--------|--------|
| 区域1 | ___ | ___ | ___ | ___ |
| 区域2 | ___ | ___ | ___ | ___ |
| 区域3 | ___ | ___ | ___ | ___ |

边界附近元素（x 在某区域边界±50px 范围内）：
- 元素名称：x=___，验证 ___ ≤ x < ___ → 属于区域___
```

---

## 验证公式

```
元素归属正确的条件：
  区域.x ≤ 元素.x < 区域.x + 区域.width

简化判断：
  元素.x >= 区域左边界 AND 元素.x < 区域右边界
```

---

## 强制规则

- [ ] 第一步记录所有区域的边界值
- [ ] 边界附近的元素（x 在边界±50px）重点验证
- [ ] 每个子元素归属通过坐标验证，不凭视觉直觉判断